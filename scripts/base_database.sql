CREATE TABLE IF NOT EXISTS USER (
    ID INT(11) NOT NULL AUTO_INCREMENT,
    NAME VARCHAR(255) NOT NULL,
    EMAIL VARCHAR(255) NOT NULL,
    CREATED_TIMESTAMP TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    MODIFIED_TIMESTAMP TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    PRIMARY KEY (ID)
    );

CREATE TABLE IF NOT EXISTS POST (
    ID INT(11) NOT NULL AUTO_INCREMENT,
    USER_ID INT(11) NOT NULL,
    TEXT_CONTENT VARCHAR(280) NOT NULL,
    IMAGE LONGBLOB,
    CREATED_TIMESTAMP TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
    MODIFIED_TIMESTAMP TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (USER_ID) REFERENCES USER(ID),
    PRIMARY KEY (ID)
    );

CREATE TABLE IF NOT EXISTS POST_LIKE (
    USER_ID INT(11) NOT NULL,
    POST_ID INT(11) NOT NULL,
    FOREIGN KEY (USER_ID) REFERENCES USER(ID),
    FOREIGN KEY (POST_ID) REFERENCES POST(ID)
    );

INSERT INTO USER (ID, NAME, EMAIL) VALUES (1, 'James', 'michael@email.com');
INSERT INTO USER (ID, NAME, EMAIL) VALUES (2, 'Karen', 'michael@email.com');
INSERT INTO USER (ID, NAME, EMAIL) VALUES (3, 'Liz', 'michael@email.com');

INSERT INTO POST (ID, USER_ID, TEXT_CONTENT) VALUES (1, 1, 'This is a text content');
INSERT INTO POST (ID, USER_ID, TEXT_CONTENT) VALUES (2, 1, 'This is a text content');
INSERT INTO POST (ID, USER_ID, TEXT_CONTENT) VALUES (3, 2, 'This is a text content');

INSERT INTO POST_LIKE (USER_ID, POST_ID) VALUES (1, 1);
INSERT INTO POST_LIKE (USER_ID, POST_ID) VALUES (3, 1);
INSERT INTO POST_LIKE (USER_ID, POST_ID) VALUES (3, 2);
INSERT INTO POST_LIKE (USER_ID, POST_ID) VALUES (2, 3);

SELECT * FROM POST;
SELECT * FROM USER;
SELECT * FROM POST_LIKE;
